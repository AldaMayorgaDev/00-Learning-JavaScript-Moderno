const frase = 'Gran venta nocturna: ¡iniciando el buen fin!';

console.log('frase: ', frase );


/*
    Método .toUpperCase()
        nombreString.toUpperCase();
    Permite convertir el string en MAYUSCULAS
*/

console.log('Método toUpperCase: ', frase.toUpperCase());

/*
    Método .toLowerCase()
        nombreString.toLowerCase();
    Permite convertir el string en minusculas
*/

console.log('Método toLowerCase: ', frase.toLowerCase());


/*
    Método .toString()
        variableNumero.toString();
    Sirve para convertir un numero en String
 */

    const precio = 300;
    console.log('Metodo toString: ', precio.toString());