/**
 * Agregando elementos con Spreed and Rest Operator
 */
 const carrito =[];

 const producto ={
    nombre: 'Monitor 32 pulgadas',
    precio: 4000
};


let resultado = [...carrito, producto];

console.table(resultado);